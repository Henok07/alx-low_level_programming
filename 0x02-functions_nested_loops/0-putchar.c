#include <stdio.h>
/**
 * main - entry point
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	char word[9] = "_putchar";

	int i = 0;

	while (i < 8)
	{
		putchar(word[i];
		i++;
	}
	putchar('\n');

	return (0);
}
